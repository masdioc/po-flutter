name: login_profile_app
description: A new Flutter project with login and profile page.

publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_dotenv: ^5.0.2
  flutter_lints: ^4.0.0
  flutter_native_splash: ^2.4.0
  provider: ^6.0.5
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    
flutter_native_splash:
  color: "#ffffff"            # Background putih
  image: assets/logo.png      # Logo splash
  android: true
  ios: true

